public sub main ()
  dim i as int: i = 0
  while (i < n)
    if (first_cond and another_cond or anotheranother) then 
      firstaction(1, 2, 3)
    else
      if (second_cond) then 
        secondaction()
      else
        thirdcondition
      endif
    endif
    i = i + 1
  wend
  hello = (1 + 2 - 3 * 4 / 5) & "String"
end sub
